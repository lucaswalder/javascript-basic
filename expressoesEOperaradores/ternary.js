// Operadores condicional (ternário)
// Dependendo da condição, nós recebemos valores diferentes


// Condição então valor 1 se não valor 2
// condition ? value1 : value2

// Exemplos

// Café da manhã top

let queijo = true
let pao = true
const niceBreakfeast = queijo && pao ? 'ótimo café' : 'café não tão bom'
console.log(niceBreakfeast)


// maio de 18
let age = 28
const canDrive = age >= 18 ? 'Pode dirigir' : 'Não pode dirigir'
console.log(canDrive)